# 项目进度

> LOG — 里程碑与变更记录。产品见 [产品设计文档.md](./产品设计文档.md)，架构见 [产品蓝图.md](./产品蓝图.md)。

## 当前阶段

**M3 体验打磨 / v1.1 Should**（已收口核心项）→ **多工具就绪**（壳侧小补丁已做）→ **v2 Dev Codec**（需求已写入产品设计，实现另立）

## 里程碑

| 里程碑 | 状态 | 说明 |
|--------|------|------|
| M1 工程脚手架 | 已完成 | settings + Appearance + 调试 DevTools |
| M1.5 Tool Host 壳 | 已完成 | Launcher 桌面、OCR 骨架、design token（v1 无跨工具 bus） |
| M2 OCR 闭环 | 已完成 | 百度 handwriting 端到端；三路输入；选框与结果；设置密钥 |
| M3 / v1.1 Should | 已完成 | 取消识别、百度 token 缓存、复制日志、keyring；AppInput/AppConfirm |
| M4 多工具验证 | 待开始 | 产品设计 §6 Dev Codec；上线前不占位 |

## 变更记录

### 2026-09-04

- **审查修复**：`ocr.settings_version` wipe 同步清 keyring + 百度 token 缓存；移除未用 `tokio-util`；`AppConfirm` i18n 默认文案；百度 `error_code` 兼容字符串
- **v1.1 Should 收口**：`cancel_recognize` + FE 取消/清除硬取消；百度 `access_token` 进程缓存；Inspector「复制日志」Tab；OCR 密码字段 OS keyring（迁移旧明文）
- **平台多工具就绪**：`AppInput` / `AppConfirm`；Launcher 关闭确认 + Esc 关菜单；产品设计写入 v2 Dev Codec 验收
- **审查小修（不含导航栈）**：Must 对齐「标题栏无返回 + BackFab」；窗控 API try/catch；左停靠收起展开钮下移避让 BackFab；结果面板顶栏允许 wrap

### 2026-09-03

- **自定义无边框标题栏**：`decorations: false` + `AppTitleBar`（设置/窗控）；`BackFab` 客户区左上返回；合并原 ToolHeader 与 Launcher 顶栏；Message 下移避让；权威文档 Must 同步
- **Message 拆分与样式**：`AppMessage` + `AppMessageHost`（TransitionGroup 进出场、flex 居中）；色条/tint/关钮/`--warning`/`--shadow-message`；队列 `MAX_VISIBLE=3`
- **OCR 粘贴与 Must 体验**：Web `paste` + `stage_image_bytes`；Rust 位图/`file_list` 回退与 `ocr.clipboard_empty`；清除可打断 busy、fit 视口、Inspector 分态、busy 提示
- **OCR 粘贴回归**：按钮走 `clipboard.read` 与 Ctrl+V 对齐；blob 预览 URL；fit 零尺寸 rAF 重试避免「有结果无图」
- **OCR 粘贴去权限窗**：按钮去掉 `clipboard.read`；Rust 补 Win CF_DIB/BITMAP；Ctrl+V 仍走 paste 事件
- **OCR 识别反馈**：画布 busy 遮罩+spinner；双击选框复制时结果面板打开则滚到对应词条
- **Launcher 卡片/菜单**：定宽网格、状态点 token、展开态不抬升；统一 `AppContextMenu` 锚点+关闭 danger
- **OCR 多引擎**：`EngineConfig` + `ocr.active_engine`；`PaddleEngine` HTTP；设置页可切换百度 / Paddle（默认百度）
- **OCR 引擎架构重构**：`engines/` + `EngineSpec` 声明式注册；`get/save_ocr_settings` bundle；`ocr.settings_version` wipe 旧键；设置 UI 描述符驱动
- **PaddleEngine 对齐官方 API**：异步 jobs + Access Token；无鉴权拉 jsonUrl；默认超时 60s；`settings_version=2`
- **文档统一 + 死代码 + 体验债**：权威文档对齐 60s/version=2/多引擎/M3 进行中；删除 toolBus/AppToolbar；窄屏布局、缩放控件、8MiB paste 上限、recognize 单飞
- **OCR 画布菜单**：去掉「⋯」按钮，仅保留右键 `AppContextMenu`
- **Launcher 卡片**：去掉常显描述，仅图标+工具名；描述改原生 `title` 悬浮
- **OCR 结果面板**：样式打磨；`ocr.inspector_placement` 可配左/右/下（默认右）
- **Launcher 图标**：去掉双套内框、提高字形占比；卡片方正收紧留白
- **Launcher 留白再收紧**：icon 44px / 卡片 100–116 / gap 12；padding 与字形 80%
- **OCR 结果面板收起**：展开按钮改为画布浮层（不占列/行），样式对齐缩放条
- **OCR 结果面板尺寸**：展开可拖调宽/高，localStorage 持久化
- **OCR 结果 Tab**：全文 / 词条互斥；reveal 自动切词条并滚动

### 2026-09-02

- **设置模块划分**：系统（`ui.*`/`app.*` → `settings/system` + Appearance/Debug Section）与工具 OCR（`ocr.*` → `tools/ocr/settings` + `useOcrSettings` + `OcrSettingsSection`）；Rust `domain/settings/{system,ocr}`；SettingsView 仅编排；行为不变
- **工具会话与菜单**：Launcher 点击弹出打开/配置/关闭；绿/红运行点；`toolSession`（open/deactivate/dispose）；OCR `useOcr` 迁入 `tools/ocr`；关闭 dispose 业务单例；系统设置不再含 OCR 段；移除 `check:isolation`
- **OCR 工作区最大化**：删除 OcrToolbar；画布右键 + ⋯（`AppContextMenu` / `useOcrActions`）；识别状态与重试进 ResultInspector；ToolHeader 去设置并改为 32px；无工具页 Esc 回桌面
- **统一顶部 Message**：`useMessage` + `AppMessageHost` 替换 Toast/Banner 与设置页底部 error；OCR 错误用 key 替换/关闭；Inspector 仅短状态 + 重试

### 2026-08-31

- **M2 OCR 闭环**：`OcrEngine` + `BaiduEngine`；`recognize_image` / 剪贴板 / 旋转 / 临时文件；`useOcr` + Canvas 同层选框 + Inspector；设置页百度密钥段；错误码 i18n
- **M1.5 Tool Host**：Android 式 Launcher 桌面（启动默认 Home）；ToolHeader 全屏工具；移除 ToolRail/BrandBar
- **隔离**：ToolDefinition 扩展；OCR Toolbar+Canvas+Inspector 骨架（后演进为画布菜单；v1 无跨工具 bus）
- **主题**：tokens/base.css 扩展 Launcher 与 OCR 语义 token；Toast/Banner Host 空壳
- **Review 修复**：KeepAlive 移出 Transition（回桌面不丢缓存）；`openTool` 校验 registry；`keepAlive` 生效；Toast 定时器清理；ToolBus handler 异常隔离；`npm run check:isolation`
- **调试入口**：移除右键/`Ctrl+Shift+I` 呼起 DevTools；改为设置页开启调试后按钮打开
- **布局与主题统一**：Banner 流式顶槽；ToolHeader/Toolbar 溢出与换行；Inspector 单滚动；画布锚点缩放；`data-scheme` 绑定 Appearance；字阶/色板 token 化
- **设置页滚动**：`.settings` 满宽整窗滚动；内容列 `.settings-inner` 居中；滚动条贴窗右

### 2026-08-28

- **M1 完成**：Tauri 2 + Vue 3 + Vite；分层 DB/settings/Appearance；Workbench 壳；export-ts；`cargo check` / `vue-tsc` 通过
- **表约定**：业务表统一 `id` 代理主键 + `created_at`/`updated_at`；`settings` 经 migration 002 升级
- **调试**：`app.debug_enabled` 可选开关；文件日志 + 设置页查看；开启后可打开 DevTools；OCR 业务未接入
